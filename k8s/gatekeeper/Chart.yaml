apiVersion: v2
name: gatekeeper
description: Gatekeeper is an automated service that claims free games from the Epic Games Store, keeping your library up to date without requiring a manual check every week.
type: application
version: 0.1.0
appVersion: "1.2.0"
